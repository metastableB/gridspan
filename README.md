# gridspan

A small library that expands a grid of parameters into typed run-configs and
runs them, with optional experiment-tracker adapters.

Design tracker (principles, open questions, todos) lives in the notes vault:
`Notes-Personal/gridspan/design.md`.

## Status

Design in progress. Scaffolding not yet started.
